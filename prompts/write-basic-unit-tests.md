Analyze this codebase and write a small set of basic unit tests for the core functions. Keep it simple:

- Unit tests only — no integration or end-to-end tests
- Use mocks where needed to isolate functions from external dependencies
- Only write a few tests per module to start (happy path + one edge case is enough)
- Use the existing test framework if one is already set up, otherwise choose the standard one for this language

After writing the tests:
1. Run them and confirm they all pass — fix any issues if they don't
2. Update the README with a section explaining how to run the tests

Do not over-engineer. The goal is a working baseline test suite, not full coverage.